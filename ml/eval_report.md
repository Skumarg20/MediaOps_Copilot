# System Evaluation — Routing, Abstention & Grounding

<!-- Generated by `npm run eval`. Do not edit by hand. -->

Generated 2026-08-25T15:40:25.982Z.

**Golden set:** 50 labelled queries — 36 that must be answered,
14 that must be abstained on. Labels are written against the fixtures, not
against observed output.

Every strategy runs the **real** routing, retrieval, agent and grounding code. Only the
path-selection policy and the grounding gate are swapped. The model runtime is the
deterministic fake adapter, so results are reproducible and measure retrieval and
gating rather than a particular model's fluency.

## Headline

Routing accuracy **100.0%** over 36 labelled cases.
False answers **3** of 14 cases that should abstain (21.4%).
Abstention F1 **84.6%**. Citation validity **100.0%**.

## Strategy comparison

| Strategy | Routing acc. | Answered | Abstained | False answers | Abstention P | Abstention R | F1 | Citation validity |
|---|---|---|---|---|---|---|---|---|
| Routed (this system) | 100.0% | 38 | 12 | **3** (21.4%) | 91.7% | 78.6% | 84.6% | 100.0% |
| Always vector | 27.8% | 26 | 24 | **3** (21.4%) | 45.8% | 78.6% | 57.9% | 100.0% |
| Always vectorless | 50.0% | 26 | 24 | **1** (7.1%) | 54.2% | 92.9% | 68.4% | 100.0% |
| Always fused | 22.2% | 41 | 9 | **5** (35.7%) | 100.0% | 64.3% | 78.3% | 100.0% |
| Random path | 47.2% | 26 | 24 | **2** (14.3%) | 50.0% | 85.7% | 63.2% | 100.0% |
| Fabricating model, gate on | 100.0% | 0 | 50 | **0** (0.0%) | 28.0% | 100.0% | 43.8% | n/a |
| Fabricating model, gate off | 100.0% | 38 | 12 | **3** (21.4%) | 91.7% | 78.6% | 84.6% | 0.0% |

*False answers* — answered a question the golden set says is unanswerable — is the metric
that matters most for an operator at 3am. Every other column is context for it.

- **Always vector** produced the same number of false answers as the routed system (3 vs 3), with routing accuracy 27.8%.
- **Always vectorless** produced 2 fewer false answers than the routed system (1 vs 3), with routing accuracy 50.0%.
- **Random path** produced 1 fewer false answers than the routed system (2 vs 3), with routing accuracy 47.2%.

## What the grounding gate is worth

Against a model that fabricates a citation on every turn, the gate withheld
**38** answers that would otherwise have reached the operator, and cut
false answers from **3** to **0**.

Ungated, **0.0%** of the answers it produced carried a citation that
resolved to retrieved evidence — every one was attributed to a source that had never been
retrieved. Gated, all of them were withheld, which is why citation validity reads `n/a`:
there were no answers left to score.

That conservatism is the trade. The same run scores an abstention F1 of
43.8%, because a model this untrustworthy costs the operator every legitimate
answer as well. The gate is doing exactly what it promises — it is not free.

This is a fabrication-injection test against a deterministic adversary, not a measurement
of how often a real model hallucinates. It bounds what the gate catches when a model does
misbehave, which is the claim the design actually makes.

## Where the routed system diverges from the golden set

| Case | Query | Divergence |
|---|---|---|
| `open-escalation` | when should I escalate to on-call | abstained when it should have answered |
| `ood-poem` | write me a poem about rendering | **answered when it should have abstained** |
| `unknown-code` | what does error code TOTALLY_MADE_UP mean | **answered when it should have abstained** |
| `unknown-job-budget` | what is the render budget for job 10000 | **answered when it should have abstained** |

## What each strategy is

- **Routed (this system)** — Deterministic pin when an anchor resolves, semantic path otherwise, with the vector to vectorless fallback and the grounding gate active.
- **Always vector** — Every query treated as semantic RAG. The naive baseline.
- **Always vectorless** — Every query treated as a keyword or record lookup.
- **Always fused** — Every query through the fused path: exact anchors, lexical and dense over one unit set, rank-fused, then expanded along the graph. Shows what routing is still worth once one path covers everything.
- **Random path** — Seeded coin flip. Establishes what path choice is worth at all.
- **Fabricating model, gate on** — Routing and retrieval unchanged, but the model fabricates a citation and paraphrases clear of the evidence. The gate is active.
- **Fabricating model, gate off** — The same fabricating model with the grounding gate disabled. The difference between this row and the one above it is what the gate is worth.

## Honest interpretation

- The golden set is **hand-labelled against the same fixtures the system serves**. It
  measures whether routing, floors and the grounding gate behave as designed on known
  data. It is not evidence about production traffic.
- Queries are templated in the same register an operator would use, but they are not real
  operator language. The abstention cases are the most transferable part, because
  "should refuse" is a property of the corpus rather than of phrasing.
- The fake model runtime removes generation quality from the measurement. That is
  deliberate: this harness tests the machinery around the model, which is the part the
  design makes claims about. It also means the four faithful rows cannot exercise the
  grounding gate at all — that adapter answers by quoting retrieved evidence, so overlap
  is high and citations always resolve. The adversarial pair exists because of that.
- The baselines share this harness's routing but not a real system's bandit. They measure
  what path selection is worth, not what a deployed alternative would score.
