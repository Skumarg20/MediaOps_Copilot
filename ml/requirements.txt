# The Python pins live in the repo-root requirements.txt, alongside the rest of
# the prerequisites. This file stays so `pip install -r ml/requirements.txt`
# keeps working (CI and older docs use that path).
-r ../requirements.txt
