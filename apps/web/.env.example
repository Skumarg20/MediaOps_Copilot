# ---------------------------------------------------------------------------
# MediaOps Copilot — Ops console
# ---------------------------------------------------------------------------

# Inlined into the client bundle at build time, so this must be the URL the
# BROWSER can reach — not a compose-internal hostname.
NEXT_PUBLIC_API_URL=http://localhost:8080
